sensitiveCodepaths:
  # Files only allowed authors can modify
  - 'install*'
allowedAuthors:
  usernames:
    # GitHub username
    - 'pjbeyer'
notificationList:
  # GitHub username or team name
  - '@pjbeyer'