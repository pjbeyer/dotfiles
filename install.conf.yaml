- defaults:
    link:
      relink: true
    create:
      mode: 0700

- clean: ['~']

- link:
    ~/.dotfiles: ""
    ~/.Brewfile:

- create:
    ~/.ssh:
    ~/Projects:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
