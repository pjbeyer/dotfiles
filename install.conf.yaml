- defaults:
    link:
      relink: true
    create:
      mode: 0700

- create:
    ~/Projects:

- clean: ['~']

- link:
    ~/.dotfiles: ""
    ~/.gitconfig:
    ~/.gitignore-global:
    ~/.Brewfile:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
