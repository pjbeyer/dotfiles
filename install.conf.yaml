- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles:
      force: true
      path: ""

- create:
    ~/.ssh:
      mode: 0700
    ~/Projects:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
